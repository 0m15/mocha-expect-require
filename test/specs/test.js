define(function(require) {

	// load any external dependency as you'd usually do
	// var dep = require('lib/dep')

	describe("a simple test", function() {
		it("should execute", function() {
			expect(1).to.be.ok()
		})
	})

})